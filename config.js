// EmailJS Configuration
// For frontend applications, you can either:
// 1. Use this config file (recommended for development)
// 2. Use environment variables with a build tool like Vite, Webpack, etc.

const emailConfig = {
    // Replace these with your actual EmailJS credentials
    publicKey: 'xdqzc3wdBc-5b6R6i',
    serviceId: 'service_p8tvaai', 
    templateId: 'template_mnx6qpo'
};

// Export for use in your main HTML file
window.emailConfig = emailConfig;
